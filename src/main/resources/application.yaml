spring:
  application:
    name: sync-service
  data:
    mongodb:
      # Inject URI via env variables configured in Secret Manager
      uri: ${MONGO_URI:mongodb://localhost:27017/sync-db}
server:
  port: 8080
